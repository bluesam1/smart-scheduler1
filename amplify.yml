version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        # Install all dependencies (npm ci includes devDependencies by default)
        - npm ci
        # Ensure Tailwind CSS v4 dependencies are installed
        - npm install @tailwindcss/postcss tailwindcss postcss --save-dev
        # Rebuild native modules for Linux (Tailwind CSS v4 requires native bindings)
        - npm rebuild @tailwindcss/oxide || true
        - npm install @tailwindcss/oxide --force || true
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend/out
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*
      - frontend/.next/cache/**/*
customHeaders:
  - pattern: '**/*'
    headers:
      - key: 'X-Content-Type-Options'
        value: 'nosniff'
      - key: 'X-Frame-Options'
        value: 'DENY'
      - key: 'X-XSS-Protection'
        value: '1; mode=block'
rewrites:
  - source: '/<*>'
    target: '/index.html'
    status: '200'

